[pytest]
testpaths = tests
addopts = -ra --strict-config --strict-markers --disable-socket --timeout=30
markers =
    integration: subprocess or multi-component integration coverage
    public_smoke: explicitly opted-in public network smoke coverage
