# 3.2 本地开发验证

分支：`codex/v3.2-ui-polish`。2026-09-16 用户授权提交、PR、CI、合并、注释标签及 Release 发布；明确无需重新下载 Release 产物。

## 变更范围

统一原生页面标题层级、卡片留白与圆角、任务状态标签、空状态、清理入口及下载反馈卡片。任务入口使用 WinUI InfoBadge，计数来自完整队列而非当前筛选；统计 queued 与所有执行阶段，暂停仍保留等待数，终态不计入。前后端版本与发行配置统一为 3.2 / 3.2.0.0，IPC 保持 v2，未修改依赖或下载执行协议。

## 验证结果

- Python 全量回归：460 passed。
- C# 真实 Python 管道：27 transport checks passed。
- WinUI Release x64：零警告、零错误。
- 原生 self-test / ui-regression：77 项状态检查、342 项界面检查、33 张截图，退出码 0。
- 175% 缩放下覆盖浅色、深色、普通窗口、800 宽紧凑导航和 640×480 最小窗口；目视检查任务卡片、侧栏角标、下载页和空筛选与清理按钮并存。
- 角标覆盖运行、等待、暂停、完成、取消、断连、空队列、筛选、跨页与紧凑导航，可访问名称包含数量。
- `git diff --check` 通过。

日志：`build/v32-pytest.log`、`build/v32-transport.log`、`build/v32-build.log`。最终原生证据及截图：`build/v32-native-delivery/`。

以上属于源码运行和确定性夹具验证，不代表正式打包、真实公网媒体下载、手机扫码、实体读屏或高对比度人工验收。未修改用户实际配置，原生回归使用隔离 profile。

## 发布验证路径

PR 与 main 的 Quality checks 验证源码；正式构建、package smoke、ZIP 审计、联合 SBOM、三份资产的 attestation 及上传 digest 核对集中在 v3.2 Release 工作流。发布后通过 GitHub API 核对 tag/commit、Release 状态、资产名称、数量、大小和 digest，不重新下载产物。保留 Codex 非空实现提交，通过合并提交进入主分支，并核对 GitHub 原生作者归属。

真实手机扫码、公网下载、实体键盘/读屏及高对比度人工验收本轮未执行，不能用本地夹具或 CI 结果替代；正式工作流结果以 GitHub Actions 为准。
