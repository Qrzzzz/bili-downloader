# v2.10 验证记录

日期：2026-09-08。基线：最新远端 main `4d9d174f3c79643e30efe410f0349b28b3211c40`。本地分支：`codex/v2.10-issue-fixes`。本轮仅修复 #24、#25、#26，并同步 2.10 版本。本文记录发布前的本地验证。用户已授权推送并发布；随后按候选提交、PR Quality、合并树一致性、main Quality、注释标签及 Release 工作流执行，远端状态以 GitHub 记录为准。

## 修复证据

| Issue | 验证内容 | 结果 |
| --- | --- | --- |
| #24 | 真实 FFmpeg 生成 4 秒 faststart MP4；截断后不能通过验证，原文件保留并下载至序号名称；新输出损坏时无成功路径 | 通过 |
| #24 | 正常 MP4、192 kbps MP3 通过，64 kbps MP3 拒绝；上述四类各覆盖正常 FFprobe 和缺失 FFprobe 的真实 FFmpeg 回退 | 8 项通过 |
| #24 | 对真实子进程触发超时或取消，确认进程已终止且回收；取消批次保留先前成功项 | 通过 |
| #25 | P1 真实完整文件复用后，P2 的 1000 个输出名称均被目录占据；另覆盖权限异常及普通逐项异常 | P1 成功路径保留；全局错误停止，普通错误继续 P3 |
| #25 | 解除准备故障后仅重试失败项，再合并原结果 | 三项成功、三条保存路径，P1 结果不变 |
| #26 | 全复用低空间、混合低空间、混合空间足够、全新低空间、全新空间足够 | 5 项通过；全复用不调用容量检查；新项目仍执行原保守预留 |

`tests/test_downloader_v210.py` 共 21 项。本地媒体检查不 mock `_matches_output_spec`；下载入口以本地文件复制替代网络，空间不足以模拟 `disk_usage` 复现，不实际填满磁盘。真实媒体用例在 FFmpeg 不可用的机器上会显式 skip，本机没有跳过。

## 完整验证

- Python：`354 passed`，日志 `build/v210-pytest.log`。包括 FFmpeg 选择边界、下载、登录态、后端生命周期及版本约束。
- 依赖一致性：`uv pip check --python .venv/Scripts/python.exe`，23 个包兼容。当前 venv 未安装 pip，使用 uv 检查既有环境，未改依赖锁。
- WinUI：使用仓库现有 `build/dotnet-sdk` 的 10.0.400 SDK，locked restore 与 Release x64 构建通过，0 warning / 0 error；输出 `BiliDownloader.v2.10.dll`。日志 `build/v210-winui.log`。
- C# 对真实 Python 管道：24 项检查通过，日志 `build/v210-transport.log`。
- `git diff --check` 通过；源码、程序集、manifest、窗口版本及 Release 工作流目标同步为 2.10 / v2.10。

## 验证边界

发布前本地未进行真实 Bilibili 媒体下载、手机扫码或原生界面人工验收；相关人工项保留待验。正式组包、package smoke、ZIP 审计、SBOM、校验和及来源验证由 Release 工作流执行，其通过状态以远端运行为准。媒体证据仅来自临时目录内的本地合成文件。完整解码检查有每文件 300 秒上限，元数据探测每次 10 秒；超时按无法验证处理。未知媒体大小沿用已有运行时磁盘错误处理，空间检查也不能保证外部进程不会在其后占用磁盘。
