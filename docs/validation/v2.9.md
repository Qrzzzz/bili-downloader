# v2.9 验证记录

日期：2026-09-05。基线：远端 `main` 的 `85caeec`（2.8 后续 IPC 修复）。本地分支：`codex/v2.9-remaining-issues`。本文记录发布前完成的本地验证。用户已授权发布 2.9，按候选提交、PR Quality、合并树一致性、main Quality、注释标签、Release 资产及来源验证顺序执行；远端事实以 GitHub 记录为准。

## 剩余 Issue 核查

2026-09-05 从 GitHub 查询的全部开放 Issue 为 #16、#18、#19；检查最新主分支后，三个根因均未修复。

| Issue | 基线问题 | 2.9 修复及证据 |
| --- | --- | --- |
| [#16](https://github.com/Qrzzzz/bili-downloader/issues/16) | 接受任务时检查 generation，但 worker 获取 lease 时重新读取最新凭据。 | 解析服务、下载请求和重试保留预期 generation，在 lease 存储事务内核对。覆盖下载/重试接受前后清除与换代、解析 worker 换代、无凭据后新登录、匿名不读存储，以及已取得 lease 的稳定副本与释放清理。 |
| [#18](https://github.com/Qrzzzz/bili-downloader/issues/18) | 扫码成功后丢弃实际验证结果，用本地 local_pending 组装终态。 | `LoginOutcome` 保留验证状态和保存代次，后端复核当前 canonical 凭据；取消、刷新、清除、替换或损坏不能显示成功。回归执行真实登录工作流和存储提交，仅用合成 QR/NAV 响应；原生 ViewModel 验证账号标题与扫码/管理入口。 |
| [#19](https://github.com/Qrzzzz/bili-downloader/issues/19) | 在途验证复核忽略凭据缺失或读取失败，可能返回旧 verified。 | 缺失返回 none，损坏/读取失败返回 invalid，换代返回 local_pending；后端与前端撤销旧解析，前端同时禁用旧重试并撤销已登录状态。覆盖普通成功保留解析。 |

## 已完成的本地验证

- Python 3.13.14，全新 `.venv` 按哈希锁安装依赖；`pytest -q` **333 passed**（新增凭据回归 30 项）；pytest 禁止 socket，所有凭据和 profile 都隔离。
- `uv pip check --python .venv/Scripts/python.exe`：23 个已安装包兼容。没有新增或升级项目依赖。
- 系统没有项目要求的 SDK，因此从微软官方元数据下载 .NET SDK 10.0.400，校验 SHA-512 后解压至忽略的 `build/dotnet-sdk`；未修改系统 SDK 或项目版本要求。
- WinUI x64 Release：NuGet locked restore、真实编译通过，**0 warning / 0 error**，输出 `BiliDownloader.v2.9.dll`。
- C# 与真实 Python 子进程管道：**24 transport checks passed**。
- 原生 `--self-test --ui-regression`：证据版本 2.9、后端连接成功，**38 项状态检查、169 项 UI 检查、20 张截图**。已查看窄窗口账号页截图，无裁切；新增状态检查直接验证成功扫码终态、账号入口、none/invalid/换代后的解析及重试撤销、迟到扫码终态的警告。
- `git diff --check` 通过；源码、程序集、manifest、窗口、测试协商版本与发行工作流目标统一为 2.9 / v2.9。README、Changelog 与发布说明同步至 2.9。

忽略目录 `build/validation-v2.9/` 保存安装、Python 回归、NuGet、WinUI 编译、管道测试日志和 `native/native-evidence.json` / 截图。原生验收中的零字节 MP4/MP3 只用于文件操作，不是真实下载结果。

## 清理与未执行边界

- 开始时只有主工作树，tracked/untracked 均干净；执行 worktree 元数据清理，将 main 快进到远端后建立 2.9 分支。未删除源码、历史、标签或用户账号资料。
- 遗留 `dist/` 为不受 Git 跟踪的 2.6 构建目录和 ZIP；核实绝对路径与目录类型后，删除命令仍被自动审批拒绝，仅返回 `blocked by policy`，所以该目录保留。清理未完成，本轮忽略的验证文件也暂留供复核。
- 没有真实手机扫码、真实账号验证或外部 Bilibili 下载；确定性测试不能代替这些人工/外部验收。
- 最终 ZIP、SBOM、摘要及来源证明由精确标签的干净提交在 Release 工作流构建和核验；确认正式发布后关闭 #16、#18、#19。真实手机扫码与外部下载保持上述未验状态，不作为已完成的发布证据。
