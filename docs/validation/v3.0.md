# v3.0 验证记录

日期：2026-09-12。基线 `f022ea8`（v2.12），实现提交 `0f5c911`。本记录区分本地实现验证与远端发布检查，不代表真实下载已全部验收。用户已授权清理中间产物并推送，进入 PR、CI、合并、注释标签与 Release 流程；远端结果以对应工作流和 Release 为准。

## 变更范围

新增 SQLite 持久任务、提交幂等、独立执行轮次、默认双任务调度、任务页面、批量解析、失败重试/继续未完成项、显式重启恢复和跨实例归属。新增任务输出暂存、同名互斥、不覆盖的原子发布、磁盘卷空间预算及 FFmpeg 处理互斥。应用 3.0 / 程序集 3.0.0.0 / IPC v2 同步；第三方依赖锁未改变。

## 自动与原生检查

| 检查 | 结果 |
| --- | --- |
| Python 3.13.14 全量回归 | 434 passed，无跳过 |
| Python 依赖一致性 | 23 个包兼容，无新增第三方依赖 |
| WinUI Release x64 / NuGet locked restore | .NET SDK 10.0.400，零警告、零错误 |
| C# 真实 Python 管道 | 27 transport checks passed |
| 原生 self-test / ui-regression | 58 项状态检查、261 项界面检查、27 张截图，退出码 0 |
| 空白与冲突检查 | `git diff --check` 通过 |

任务相关确定性检查覆盖：并发上限与补位；取消 A 不影响 B；暂停队列与降低上限不打断在途工作；跨实例认领与中断恢复；提交标识在去重任务完成后仍保持幂等；旧解析注册项不因新输入而丢失；批量非法地址不进入上游解析；凭据代次变化在联网前阻断；分 P 身份/严格画质变化不静默改变规格；重试只执行失败项并保留成功结果；线程启动与终态存储失败收尾；同名输出互斥及外部写入时不覆盖文件；媒体处理资源等待可取消；空间预算共享、增长检查与释放；移除记录保留文件。

C# 队列检查调用生产 Backend、TaskManager 和 SQLite，通过真实管道创建三个任务，在双并行中取消一个并启动等待任务，继续解析新视频，随后安全关闭并从新进程读取 interrupted 记录。上游元数据与媒体执行是明确的夹具，不代替公网媒体验证。

原生检查直接运行 WinUI 窗口，在 175% 缩放下检查普通尺寸与 640×480 逻辑尺寸。目视检查任务页浅色、深色窄窗口与筛选空状态；记录阶段均只有一个原生窗口，关键控件未横向溢出。状态检查另通过真实生产队列与夹具媒体执行覆盖批量加入、并行时输入可用、独立取消、读取结果详情及继续添加新视频。

本机检查日志原位于 `build/v30-pytest-final.log`、`build/v30-winui-final.log`、`build/v30-transport-final.log`；原生证据原位于 `build/v30-native-final/native-evidence.json` 及同目录截图。按用户要求，2026-09-12 已将这些日志、两轮验证截图、临时脚本、占位媒体、构建输出与缓存移入回收站，共 27 个清理目标、785 个文件、254,036,995 字节；保留正式测试源码、Python 虚拟环境和 .NET SDK。截图与测试过程属于本地 dirty 构建，不是正式发布资产。最后新增的去重提交标识和空间增长边界由最终 Python 全量回归覆盖。

## 真实公网检查：未通过

使用独立临时配置、匿名模式和现有受控 FFmpeg 路径，尝试两个既有公开测试视频：`BV1GJ411x7h7` 的 360p MP4 与 `BV1kkbC6eEgm` 的 MP3。视频时长均不超过 5 分钟；不读取或复制本机登录凭据。

两个视频均成功取得元数据，进入独立任务调度，观察到最多 2 个活动任务。但媒体下载均返回网络证书校验错误，最终状态为 failed，成功输出为 0。因此不能认定真实并行媒体下载、合并转码或公开源取消恢复通过。

未关闭 TLS 校验、未更换绕过路径、未伪装成功。记录原位于 `build/v30-public-queue.json` 和对应日志，现已按要求移入回收站；临时配置及媒体工作目录也已清理。仍待在可正常校验证书的网络环境中完成两项真实任务的并行、结果校验及取消/恢复验收。根据当前发布检查清单，公网 smoke 属于手动诊断，不作为合并或发布门槛；本次发布授权不改变上述未通过结论。

本次未执行真实手机扫码、实体键盘/剪贴板全链路、读屏或高对比度人工验收。正式组包、package smoke、归档审计、SBOM、三份资产的 attestation 与远端摘要核验由精确标签的 Release 工作流执行；工作流结果不能替代这些人工或公网未验收项。

## Codex 原生 Contributors

用户要求 3.0 的实现归属出现在 GitHub 原生 Contributors。已通过 GitHub API 核对 `chatgpt-codex-connector[bot]`，公开 ID 为 `199175422`；推送前远端 Contributors 只有 `Qrzzzz`。

本版真实实现提交 `0f5c911` 已使用 `Codex <199175422+chatgpt-codex-connector[bot]@users.noreply.github.com>` 作为作者，保留实际维护者为 committer，最终进入默认分支时必须保留该归属。发布后核对 commit API 的 `author.login` 与 Contributors/API，缓存未刷新时记录待验证。仅有本地提交、分支或文档署名不能声称已经出现。
