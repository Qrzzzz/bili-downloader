# v2.11 验证记录

日期：2026-09-12。基线为与远端 main 一致的 `b4b5ccb38d0d50bc278ee790a1193ae0f43811ec`（v2.10）。分支：`codex/v2.11-issue-fixes`。仅处理开发开始时仍开放的 #28、#29。本文记录发布前的本地验证。用户随后授权推送并进入 Release 流程，按候选提交、PR Quality、合并树一致性、main Quality、注释标签和 Release 工作流执行；远端状态以 GitHub 记录为准。

## 开发前清理

已将 21 个旧中间产物目标移入 Windows 回收站，合计 233.88 MiB，包括旧 bin/obj、Python/pytest 缓存、2.10 验证日志及 20 张截图、两处已脱离 Git 的空工作树和两处空临时目录。路径与 reparse point 检查通过；没有源文件或未提交工作被移除。保留现有 Python venv 与 .NET SDK 供本轮验证使用。

本机清理清单和完成状态记录于 `build/v211-cleanup-plan.json`、`build/v211-cleanup-result.json`。所有目标均成功回收，没有永久删除或清空回收站。后续验证产物属于本轮新生成内容。

## 修复与回归证据

| Issue | 检查 | 结果 |
| --- | --- | --- |
| #28 | 线程构造失败、start 失败、worker 内部异常、正常完成 | 每个请求一次响应；接受后恰有一个合法终态，ACK 在前，active 释放 |
| #28 | 启动失败后的取消、后续重操作、shutdown/wait | 取消返回 already_finished；后续任务完成；不 join 未启动的线程 |
| #28 | C# 生产 BackendClient 对真实 Python Backend/serve，注入一次构造或启动故障 | 已接受 Task 明确失败；连接继续可用，下一任务成功，退出完成 |
| #28 | 真实 WinUI ApplicationSession 对同一故障后端 | Busy、ActiveMethod、取消入口清理，错误显示，后续任务正常完成 |
| #29 | MP4/MP3：P1 完成，P2 后处理 started 后取消，P2 安全完成 | completed / completed / cancelled；保留两条路径，P3 没有启动 |
| #29 | 延迟取消后的损坏输出、验证超时 | P2 failed，不暴露成功路径，保留 P1，P3 cancelled |
| #29 | 无取消、后处理前取消、普通完整解码验证中取消 | 正常全部完成；可立即响应的取消仍中止当前项，不启动后续项 |
| #29 | 生产 IPC DTO 与原生结果 UI | 两条保存路径与逐 P 状态一致；第二个完成文件可选择，打开文件按钮可用 |

修复前的定向回归在生产基线上观察到构造/启动遗留 active、join 异常，以及 MP4/MP3 当前已完成文件被记录为 cancelled。修复后相同检查通过。新增 `tests/test_backend_v211.py` 5 项和 `tests/test_downloader_v211.py` 12 项。

媒体回归使用真实 FFmpeg 生成 4 秒 H.264/AAC MP4、1 秒 192 kbps MP3，执行生产元数据探测和完整解码；没有将 `_matches_output_spec` 替换为恒真。远端格式规划及 yt-dlp 传输/后处理回调以本地夹具替代。超时与验证中取消使用可控的真实子进程，既有 2.10 回归继续覆盖探测子进程终止/回收、FFprobe 缺失回退、截断媒体、错误码率和已有成功结果保留。

原生结果回归的路径是明确标记的占位文件，只证明 UI 映射与文件选择/按钮状态，不证明实际播放或 Bilibili 下载。线程故障通过受限测试入口注入，没有真实耗尽系统资源。

## 完整检查

- Python 3.13.14：全量 `371 passed`，没有跳过；`build/v211-pytest.log`。最终版本资源输入同步后，30 项发布约束再次通过，见 `build/v211-version.log`。
- 依赖一致性：`uv pip check --python .venv/Scripts/python.exe`，23 个包兼容，依赖锁未改。
- .NET SDK 10.0.400：NuGet locked restore 与 WinUI Release x64 构建通过，零警告、零错误；`build/v211-winui.log`。
- C# 真实管道测试：`26 transport checks passed`，包含两个新增生产后端故障场景；`build/v211-transport.log`。
- 原生 WinUI `--self-test --ui-regression`：退出码 0，46 项状态检查、187 项界面检查、22 张截图；所有截图阶段只有一个原生窗口。已目视核对 MP4/MP3 延迟取消结果，均显示“已取消 · 成功 2 / 3”，第二个输出仍可选择。
- 原生证据：`build/v211-native/native-evidence.json`；版本 2.11、GitCommit 为基线提交、BuildDirty 为 true，后端连接正常。截图为 `result-deferred-mp4.png` 和 `result-deferred-mp3.png` 等。
- `git diff --check` 通过；源码、前端窗口、程序集、manifest、测试及 Release 工作流目标为 2.11 / v2.11。进入发布流程时同步中英文 README 和 CHANGELOG。

## 验证边界

发布前本地未执行真实 Bilibili 媒体下载或人工播放，相关实际下载/取消验收仍待进行；未涉及登录协议与凭据实现，也未执行真实手机扫码。正式组包、package smoke、SBOM、资产审计、校验和及来源验证由精确标签触发的 Release 工作流执行；本地合成媒体与 UI 夹具不能替代这些远端发行证据或真实 Bilibili 下载验收。

延迟取消后的完整解码仍有每文件 300 秒上限，元数据探测每次最多 10 秒。已开始后处理的当前文件通过这些检查后才能报告成功；普通验证保持取消检查，取消标记始终保留用于阻止下一分 P。
