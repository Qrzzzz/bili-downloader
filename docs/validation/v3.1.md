# v3.1 验证记录

日期：2026-09-12。基线 main `861170c42f007ea80612dd839327934ed2454191`（v3.0），分支 `codex/v3.1-download-preferences`。用户授权实现下载模式与画质偏好，并沿用 3.0 的清理、推送、PR、CI、合并、注释标签与 Release 资产核验流程。远端发布结果以精确提交上的 Quality checks 和 Release 工作流为准。

## 范围与结果

新增自动记忆开关、保存的 MP4/MP3 模式和画质高度。设置页可直接调整；指定画质不可用时提示重新选择，MP3 保留视频画质，批量逐项匹配，已入队任务规格保持原快照。schema 1 与旧配置兼容，应用 3.1 / 程序集 3.1.0.0 / IPC v2 一致；未修改第三方依赖锁、媒体执行、登录协议或 FFmpeg 调用。

| 检查 | 结果 |
| --- | --- |
| Python 3.13.14 全量回归 | 460 passed，无跳过 |
| Python 环境一致性 | uv pip check：23 个包兼容 |
| WinUI Release x64 / NuGet locked restore | .NET SDK 10.0.400，零警告、零错误 |
| C# 真实 Python 管道 | 27 transport checks passed；包含真实后端重启读取偏好 |
| 原生 self-test / ui-regression | 71 项状态检查、301 项界面检查、29 张截图，退出码 0 |
| 显示与布局 | 175% 缩放；普通窗口及 640×480 逻辑尺寸；浅色、深色；单一原生窗口，关键控件无横向溢出 |
| 格式检查 | git diff --check 通过 |

配置测试覆盖旧无版本号/schema 1 配置迁移、MP3/分辨率/开关往返、整数与布尔值严格区分、畸形值拒绝、损坏偏好单字段回退、部分更新保留其他字段、原子保存失败不改变内存或磁盘、关闭自动记忆后忽略迟到写入。

前端状态检查覆盖主动选择自动保存、格式 ID 改变后按高度匹配、严格画质缺失不降档且不覆盖偏好、MP3 忽略视频画质要求、快速连续切换保留最后选择、设置目录草稿与自动记忆按字段合并、关闭自动记忆后保留已保存默认值、设置更新回到最高可用。生产队列配合明确的元数据/媒体夹具覆盖：批量中一项缺少指定画质时不入队，其余项目正常加入；改为最高可用后加入剩余项，原任务仍保持 1080p。

原生检查在真实 XAML 页面上展开“下载偏好”，使用原生 Toggle 自动化接口及 ComboBox 选择更新绑定，通过保存按钮的 Invoke 接口写入真实后端，并读回配置。检查 Toggle/ComboBox 自动化语义、键盘焦点及跨页草稿。已目视检查偏好区浅色与深色窄窗口截图；其他窗口状态沿用现有原生回归。未把组件内部语义检查等同于读屏或实体键盘全链路验收。

## 清理与证据边界

验证完成后按用户指定流程，将 18 个工作区清理目标、784 个生成文件、254,501,050 字节（约 243 MiB）移入回收站，并确认原路径已不存在。包含两轮原生验证目录及截图、占位媒体、隔离配置、测试/构建日志、bin/obj、pytest 与 Python 缓存；正式测试源码、Python 虚拟环境和 .NET SDK 保留。

本地日志原位于 `build/v31-pytest.log`、`build/v31-transport-final.log`、`build/v31-winui-final.log`；最终原生证据原位于 `build/v31-native-final/native-evidence.json` 及同目录截图，均已移入回收站。这些属于本地开发验证，不是正式发行资产。

本版未重新执行真实公网媒体下载、手机扫码、读屏、高对比度或实体设备人工验收。3.0 已记录的本机 TLS 媒体下载失败仍属于未解决的外部验收项；偏好与队列夹具通过不能替代真实媒体结果。按当前发布清单，公网 smoke 为手动诊断，不作为发布门槛。

正式 Windows x64 / Python 3.13 构建、package smoke、ZIP 审计、联合 SBOM、SHA256SUMS、三份资产的 attestation 及上传 digest 校验由标签 v3.1 的 Release 工作流执行。发布后用 GitHub API 核对状态、标题、tag/commit、资产数量/大小/digest；不重复下载已由工作流核验的资产。

实现提交继续使用可关联的 Codex 作者邮箱，并通过保留提交的合并进入默认分支。发布前已读取原生 Contributors API，`chatgpt-codex-connector[bot]` 已有 1 次贡献；本版贡献归属在推送合并后再按 commit API 核实。
