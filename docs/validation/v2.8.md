# v2.8 验证记录

日期：2026-09-05。本文记录正式发布前完成的本地候选验证；真实外部 Bilibili 下载仍待具备合法内容授权时人工完成。用户已明确授权完成 commit、push、PR、merge、注释标签、GitHub Release、关闭对应 issue，并在发布验证后清理中间文件与本地 worktree。远端发布事实以 GitHub 上的合并提交、标签、工作流与 Release 资产为准。

## 基线与范围

- worktree 初始状态为 detached、干净的 `e9986d5cf82ff82d6a92691201a6f8991cebb3c1`，随后从该提交建立 `codex/v2.8`。
- 刷新远端后，`origin/main` 与 PR #20 merge commit 均为该提交；注释标签对象 `b790962e481ec1652569ef35dd2e213e55f8eec3` 解引用到该提交。
- 开始开发时，GitHub 最新正式 Release 为非 draft、非 prerelease 的 `Bili Downloader Lite v2.7`；Release workflow 33905493393 在该提交成功，资产为 ZIP、SBOM、SHA256SUMS。
- 开始开发时 #12、#13 为 CLOSED，#14、#15、#17 为 OPEN。本版只修改后三项；#16、#18、#19 不在范围。

远端查询 JSON 与 fetch 日志位于忽略目录 `build/validation-v2.8/baseline/`，不提交用户配置、凭据或媒体。

## v2.7 当前基线复现

三项均在修改生产代码前、当前 v2.7 精确提交上重新运行；旧审计材料只提供复现思路，没有把 v2.6 结果当成本轮证据。

| issue | 当前基线结果 | 证据边界 |
| --- | --- | --- |
| #14 | 锁定 yt-dlp 2026.08.19 使用真实文件名、已有文件判断和 hooks；预置旧占位字节后请求严格 1080p，日志为 `has already been downloaded`，结果 completed，旧字节未变。 | 占位字节不是低画质真实媒体；它只证明跳过与误报链路。 |
| #15 | 真实 `BilibiliBaseIE.extract_formats()` 生成 durl，真实 yt-dlp 归一化并选择 format 16 / MP4；vcodec/acodec 均为 null，生产预检返回 format_unavailable。 | 合成 play_info、完全禁网；这是确定性兼容证据，不代表线上样例当前返回 durl。 |
| #17 | PowerShell/.NET 直接编译生产 BackendClient，真实 Python 管道先 accepted，再发送 completed 缺 result；连接断开，但 operation Task 在 shutdown 后仍未完成。 | 故障管道不是正常后端故障率证据。 |

原始结果为 `downloader-repro.json`、`downloader-repro.log`、`transport-repro.json`、`transport-repro.log` 及相应复现源码。

## 候选实现与当前证据

| 范围 | 实现边界 | 当前证据 |
| --- | --- | --- |
| 输出身份与复用 | 规格身份进入清理、UTF-8 限长后的文件名；同规格已有文件通过受控 FFprobe/FFmpeg 验证后直接复用，不再重新下载源流；不匹配文件原样保留并选择 `-2` 等名称。新下载结果只接纳 yt-dlp 实际报告且符合预期规格路径的文件。 | 下载与 FFmpeg 定向回归 **87 passed**。覆盖锁定版 yt-dlp 原生已有文件判断、不同画质、MP4/MP3 同规格复用且不触发 transport、损坏同名保留、失败重试路径、多 P、FFprobe 缺失/损坏回退及绝对 FFmpeg 边界。 |
| durl codec | 在 yt-dlp 归一化前，只标记 `BiliBili` 原始结果中匹配 legacy durl 窄形状的格式；显式 codec 分类与普通未知格式保持 fail closed。 | 真实 `BilibiliBaseIE` + 真实 yt-dlp 归一化测试覆盖 MP4/MP3；另覆盖明确合流、纯视频、纯音频、DASH 配对、未知非 durl、缺高度和多 P。 |
| IPC 终态 | terminal method/result/error 在 sequence 记录、Event 发布和 TryRemove 前验证；协议错误由 FailConnection 完成全部 pending/accepted 调用。并发写入撞上断连关闭时，当前调用也复用已记录根因明确完成。 | **24** 项 C# 测试通过真实 Python 管道覆盖 completed/failed/cancelled 畸形结构、非法 seq、EOF、多 operation、重复/迟到、id/seq 复用与 shutdown；accepted 与 pending 并存的关闭竞态连续运行 20 次。原生状态验收确认 Busy/finally、错误提示和关闭均收尾。 |

## 已完成的本地 Gate

- 在全新 `.venv` 中按 `requirements-dev.txt` 使用 `--require-hashes --only-binary=:all:` 安装；Python 3.13、锁定 yt-dlp 2026.08.19。三份依赖锁一致性包含在全回归中，结果 **303 passed**；`pip check` 为 `No broken requirements found.`。
- 使用 .NET SDK 10.0.400 对测试工程和 WinUI 工程执行 NuGet locked restore；WinUI x64 Release 编译成功，**0 warning / 0 error**，输出 `BiliDownloader.v2.8.dll`。
- C# 传输可执行测试使用本轮 `.venv` 的真实 Python 子进程与 JSONL 管道，**24 transport checks passed**。正常终态只完成一次；畸形终态不发布给 UI，全部已接受/待响应任务明确失败，EOF、并发写入关闭和 shutdown 均有界完成。
- 重新运行编译后的 `BiliDownloader.v2.8.exe --self-test --ui-regression`，使用隔离的 `APPDATA`、`LOCALAPPDATA`、输出目录和源码测试后端。进程退出码 0；证据记录版本 2.8、后端已连接、**24** 个状态检查、**169** 个 UI 检查、**20** 张原生截图。畸形终态会释放 Busy、不伪装成功、显示协议失败并完成关闭。
- 确定性媒体验收用实际选中的 FFmpeg/FFprobe 生成并探测带 AAC 的 360p/720p MP4，再让真实锁定版 yt-dlp 的选择、hooks、命名和 MP3 后处理运行；唯一替换的是网络 transport（本地复制已生成媒体），并硬性禁止 socket 连接。不同画质路径分离；MP4 和 MP3 同规格均复用；不相关同名文件保持字节不变且输出到 `-2`；最终 MP4 有音视频轨与准确高度，MP3 只有音轨。证据写出后，合成媒体和隔离配置目录已删除。
- 前后端及全部发行约束均为 2.8 / v2.8；最终执行 `git diff --check`，并逐项审阅 tracked/untracked 范围与敏感数据。依赖锁、打包脚本、凭据和 #16/#18/#19 实现未改。

忽略目录 `build/validation-v2.8/` 保存依赖安装、`pytest-download-final.log`、`pytest-full-final.log`、`pip-check-final.log`、NuGet restore、`winui-build-final.log`、`transport-final.log`、`native-final-current/native-evidence.json`、截图及 `synthetic-media.json`。原生验收写出的两个零字节示例文件只测试结果区文件操作，不是媒体；确定性媒体证据则来自实际容器、实际 FFmpeg/FFprobe，二者均不等于外部 Bilibili 下载。

## 未执行与外部/人工边界

- 没有使用任何先前的一次性内容授权，也没有获得本轮可下载内容，因此未访问 Bilibili 执行真实外部 MP4、MP3、分 P、改选画质、取消或重试。这些项目保持 **PENDING / 人工待验**，不能用禁网测试、合成 play_info 或本地媒体替代。
- `build.ps1`、spec、资源与打包依赖均未修改；本地没有把 dirty 源码构建冒充正式资产。ZIP、package smoke、SBOM、摘要与 attestation 由精确 `v2.8` 标签触发的 Release 工作流从干净提交生成和验证。

## 版本与兼容边界

版本同步为源码/程序集/manifest/窗口/发行约束 2.8 / v2.8，两段版本格式不变。IPC 1、配置 schema 1、DPAPI 与 Cookie/FFmpeg 安全边界保持兼容；没有依赖升级或 UI 改版。

## 发布与清理边界

PR #21 的候选 Quality 已通过；squash 后 tree 与候选 tree 一致。main Quality 首次运行 33937829294 在 C# 故障夹具中命中一个此前仅表现为时序抖动的写入关闭竞态：输入管道因畸形终态断开时，待响应请求的 `FlushAsync` 可能先抛裸 `ObjectDisposedException`。当时尚未创建 v2.8 标签或 Release。修复统一复用已记录的协议失败并显式完成当前调用，同时把该竞态重复 20 次；后续发布必须以修复提交的 PR/main Quality 成功为准。

发布按候选提交、PR Quality、squash merge tree 对比、main Quality、注释标签、Release 工作流、Release API/资产摘要/来源证明复核的顺序执行。只有远端三份资产及来源绑定全部确认后才关闭 #14、#15、#17。随后删除本地忽略的构建、测试、截图和验证产物并移除开发 worktree；保留已提交源码、正式测试、文档、Git 历史、标签和 Release。
